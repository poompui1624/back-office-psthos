/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
first()
hasRole('super_admin')
/.fleet
/.idea
/.nova
/.vscode
/.zed

# The agent's config holds a bearer token in clear text. The pattern is wide
# on purpose: a "config.example copy.json" left beside it once carried a live
# token and was not matched by a narrower rule.
agent/**/config*.json
!agent/config.example.json
