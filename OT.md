# รายงานการดำเนินการ: ระบบจัดตารางเวรทุกหน่วยงาน

วันที่ดำเนินการ: 29 พฤษภาคม 2569

## เป้าหมาย

เพิ่มและปรับปรุงระบบจัดตารางเวรให้รองรับการใช้งานภาพรวมทุกหน่วยงาน โดยอ้างอิงรูปแบบหน้าจอจากภาพตัวอย่าง และต่อยอดจากโมดูล `duty-schedules` เดิมของ Laravel เพื่อไม่ให้ระบบแตกเป็นหลายส่วน

## สิ่งที่ดำเนินการแล้ว

1. ปรับหน้า `duty-schedules.index` ให้เป็นหน้า Dashboard/Overview สำหรับระบบเวร
   - Header แบบ modern SaaS ใช้สีหลัก `#02abff`
   - รองรับ responsive mobile/tablet/desktop
   - เพิ่ม filter เดือน, ปี พ.ศ., หน่วยงาน, กลุ่มงาน, ค้นหาบุคลากร และจำนวนรายการต่อหน้า
   - เพิ่มปุ่มใช้งานหลัก: ตารางเวร, จัดเวรหลายรายการ, เพิ่มเวร, Excel, พิมพ์

2. เพิ่ม Stat cards สำหรับภาพรวมรายเดือน
   - จำนวนวันในเดือน
   - จำนวนบุคลากรที่ใช้งาน
   - จำนวนเวรที่จัดแล้ว
   - จำนวนเวรที่ยืนยันแล้ว
   - จำนวนวันลาที่อนุมัติ
   - จำนวน OT ในเดือน
   - จำนวนรายการยกเลิก

3. เพิ่มส่วนวิเคราะห์ภาระงาน
   - การกระจายประเภทเวรตาม `shift_types`
   - ภาระงาน Top 10 ของบุคลากรตามจำนวนเวรในเดือน
   - Empty state เมื่อยังไม่มีข้อมูลเวร

4. ปรับตารางรายการเวรให้ใช้งานง่ายขึ้น
   - แสดงวันที่, บุคลากร, หน่วยงาน, เวร, เวลา, กลุ่มงาน, สถานะ และปุ่มจัดการ
   - เพิ่ม hover state
   - เพิ่ม empty state ที่แนะนำให้เริ่มจัดเวรหลายรายการ
   - ปรับ pagination ให้ดูสะอาดขึ้น

5. เพิ่มข้อมูลสรุปใน `DutyScheduleController`
   - ย้าย logic สรุปข้อมูลไว้ใน controller
   - Query ข้อมูลตามเดือน/ปี/หน่วยงาน/กลุ่มงาน
   - นับ OT จากประเภทเวรที่มี code/name เกี่ยวกับ `OT` หรือคำว่า `ล่วงเวลา`

6. เพิ่ม test สำหรับหน้า Dashboard ตารางเวร
   - ตรวจสอบว่า user ที่มีสิทธิ์ `duty.view` เข้าใช้งานหน้า `duty-schedules.index` ได้
   - ตรวจสอบข้อความหลักและ empty state

## ไฟล์ที่แก้ไข/เพิ่ม

- `app/Http/Controllers/DutyScheduleController.php`
- `resources/views/duty-schedules/index.blade.php`
- `tests/Feature/DutyScheduleDashboardTest.php`
- `OT.md`

## ผลการทดสอบ

ผ่านทั้งหมด

```bash
vendor\bin\pint --dirty --format agent
php artisan test --compact tests\Feature\DutyScheduleDashboardTest.php
php artisan test --compact
npm run build
```

ผลรวม test ล่าสุด: 34 tests ผ่านทั้งหมด, 80 assertions

## หมายเหตุ

- ยังไม่ได้เพิ่ม algorithm จัดเวรอัตโนมัติแบบเต็ม เพราะระบบเดิมมีหน้าสร้างเวรหลายรายการอยู่แล้ว จึงเชื่อมปุ่มไปใช้ workflow เดิมก่อนเพื่อความปลอดภัยของข้อมูล
- ค่า OT ตอนนี้นับเป็นจำนวนรายการเวร OT ยังไม่ได้คำนวณเป็นเงิน เพราะยังไม่พบตาราง/สูตรเรต OT เฉพาะในระบบ
- พยายามเปิดตรวจหน้าเว็บด้วย Browser plugin แล้ว แต่เครื่องมือ browser ใน Codex ล้มเหลวจาก sandbox ฝั่ง Windows จึงยืนยันด้วย Pest และ Vite build แทน

## ข้อเสนอแนะถัดไป

1. เพิ่มหน้าตารางเวรแบบ grid รายวัน/รายคน คล้ายภาพตัวอย่าง เพื่อจัดเวรได้เร็วขึ้น
2. เพิ่มตารางกำหนดสูตร OT เช่น อัตราวันทำงาน, วันหยุด, เวรดึก และคำนวณเงิน OT ได้จริง
3. เพิ่ม rule จัดเวรอัตโนมัติ เช่น ห้ามเวรดึกติดกัน, กระจายเวรเท่า ๆ กัน, ตรวจวันลา, ตรวจวันหยุด
4. เพิ่ม export PDF สำหรับตารางเวรรายเดือน
5. แยก service สำหรับคำนวณ dashboard และ workload เพื่อลดภาระของ controller เมื่อระบบใหญ่ขึ้น
